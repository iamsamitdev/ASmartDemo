import 'package:flutter/material.dart';

class PinCode extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Scaffold(
        appBar: AppBar(
          title: Text('กรุณากรอกรหัสเข้าใช้งานจากอีเมล์'),
        ),
        body: Container(
          child: Text("Pincode page comming soon...."),
        ),
      ),
    );
  }
}